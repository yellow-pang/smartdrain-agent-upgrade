# SmartDrain Codex Guidelines

SmartDrain은 팀 프로젝트 MVP에서 출발했지만, 현재 저장소는 개인 Fork 기반으로 아키텍처 리팩터링과 기능 고도화를 진행하는 작업 공간입니다.

기존 MVP의 맥락을 존중하되, 새로운 변경은 다음 기준으로 판단합니다.

- 유지보수성
- 실행 재현성
- 서비스 간 책임 분리
- 테스트 가능성
- AI 에이전트를 활용한 작업 확장성

## 1. 작업 시작 전 확인

실제 파일을 수정하는 작업은 먼저 다음 명령으로 현재 상태를 확인합니다.

```bash
git status --short --branch
```

확인할 내용은 다음과 같습니다.

- 현재 브랜치
- 미커밋 변경
- 사용자에게서 이미 존재하던 변경
- 이번 요청으로 수정할 예상 범위

단순 설명, 코드 분석, 명령어 안내처럼 파일을 변경하지 않는 요청에는 Git 작업 절차를 적용하지 않습니다.

문서만 보고 판단하지 않고 다음 항목을 우선 확인합니다.

- 실제 디렉터리와 파일 구조
- 실행 스크립트
- 패키지 및 의존성 파일
- 테스트 설정
- Docker 및 CI 설정
- 현재 동작하는 구현

문서와 구현이 다르면 현재 실행 가능한 구현을 기준으로 판단하고, 발견한 차이를 사용자에게 보고합니다.

구현 전에 사용자 요청과 완료 조건을 짧게 정리하고, 구현이 끝난 뒤 요청한 결과와 실제 결과를 비교합니다.

사용자가 의도와 다른 결과라고 지적하면 바로 임의 수정하지 않고 다음을 먼저 확인합니다.

- 기대 결과
- 실제 결과
- 현재 Git diff
- 원인 후보

같은 버그가 재발할 가능성이 있으면 문서 규칙 추가보다 회귀 테스트 또는 검증 가능한 재현 절차를 우선합니다.

한 번 발생한 모든 실수를 `AGENTS.md`에 무조건 추가하지 않고, 반복 가능하거나 프로젝트 전체 기준으로 의미 있는 원칙만 반영합니다.

## 2. 기본 변경 원칙

- 사용자가 지정한 파일이나 디렉터리 범위를 벗어나 수정하지 않습니다.
- 범위 밖 변경이 반드시 필요하면 변경 이유, 대상 파일, 예상 영향을 먼저 보고합니다.
- 사용자에게서 이미 존재하던 변경을 덮어쓰거나 되돌리지 않습니다.
- 요청 해결에 필요하지 않은 전면 재작성은 피합니다.
- 미래 사용 가능성만을 위한 과도한 추상화와 공통화를 피합니다.
- 기존 프로젝트 패턴을 우선 재사용합니다.
- 구조 변경과 동작 변경은 가능한 한 분리합니다.
- 변경은 검증 가능한 작은 단위로 나누어 진행합니다.

## 3. 사용자 승인 필수 변경

다음 변경은 실제 적용 전에 사용자 승인을 받습니다.

- 서비스 간 책임 이동
- 디렉터리 또는 파일의 대규모 이동
- API 계약의 하위 호환성을 깨는 변경
- WebSocket 메시지 계약 변경
- AI callback 계약 변경
- DB schema 변경
- Alembic migration 생성 또는 수정
- 패키지 추가, 교체 또는 삭제
- 환경변수 구조 변경
- Docker 또는 Docker Compose 변경
- Nginx 설정 변경
- Jenkins 또는 GitHub Actions 변경
- 인증 및 권한 처리 방식 변경
- 비밀값 저장 또는 전달 방식 변경

다음 작업은 동일한 책임 범위 안에서 동작을 바꾸지 않는 경우 별도 승인 없이 진행할 수 있습니다.

- 파일 분리
- 함수 또는 변수 이름 개선
- 타입 개선
- 테스트 추가
- 중복 코드 정리
- 작은 범위의 리팩터링
- 문서와 실제 구현의 불일치 수정

## 4. 브랜치 원칙

실제 파일을 변경하기 전에 현재 브랜치가 요청 목적과 맞는지 확인합니다.

현재 브랜치가 다른 작업을 위한 브랜치이거나 요청과 맞지 않으면 파일을 수정하지 않고 새 브랜치 이름을 추천합니다.

저장소에 별도 규칙이 없으면 다음 형식을 사용합니다.

```text
feat/<기능명>
fix/<수정대상>
refactor/<개선대상>
test/<테스트대상>
docs/<문서대상>
chore/<설정대상>
```

사용자가 명시적으로 요청하지 않는 한 다음 작업은 수행하지 않습니다.

- 브랜치 생성
- 브랜치 체크아웃
- 브랜치 삭제
- 브랜치 병합

브랜치 이름을 안내하거나 체크아웃 여부만 확인한 단계에서는 커밋 메시지를 제안하지 않습니다.

## 5. 서비스별 지침

작업 대상에 따라 다음 파일을 함께 확인합니다.

- Frontend: `frontend/AGENTS.md`
- Backend: `backend/AGENTS.md`
- AI Service: `ai_service/AGENTS.md`

하위 디렉터리의 `AGENTS.md`는 루트 규칙을 보완하며, 해당 영역에서는 더 구체적인 하위 규칙을 우선 적용합니다.

## 6. Skill 사용 기준

작업 종류에 따라 다음 Skill을 사용합니다.

- 복잡한 변경 계획: `.agents/skills/task-planning/SKILL.md`
- 승인된 계획 구현: `.agents/skills/implementation-workflow/SKILL.md`
- 작업 결과 기록: `.agents/skills/steps-documentation/SKILL.md`
- PR 문서 작성: `.agents/skills/pr-documentation/SKILL.md`

다음 조건에 해당하면 구현 전에 `task-planning` Skill을 사용합니다.

- 여러 파일이나 여러 계층에 걸친 동작 변경
- 둘 이상의 서비스에 영향을 주는 변경
- API, DB, WebSocket 또는 callback 변경
- 실행 방식이나 데이터 흐름에 영향을 주는 변경
- 사용자 승인이 필요한 변경
- 구현 대안이 둘 이상 존재하는 변경
- 대규모 구조 변경 또는 리팩터링

문서와 구현의 불일치를 바로잡는 작은 문서 교정처럼 구조나 동작을 바꾸지 않는 작업은 Plans 문서 없이 진행할 수 있습니다.

## 7. 의존성과 생성 파일

새 의존성을 추가하기 전에 다음을 확인합니다.

1. 기존 의존성으로 해결 가능한지
2. 표준 라이브러리나 프레임워크 기본 기능으로 해결 가능한지
3. 추가 목적이 명확한지
4. 번들 크기 또는 런타임에 미치는 영향
5. 유지보수 상태와 대안

패키지 변경은 사용자 승인 후 진행합니다.

lockfile은 실제 패키지 변경이 발생한 경우에만 수정합니다.

다음 생성 파일은 Git 추적 대상으로 추가하지 않습니다.

- `.next/`
- `node_modules/`
- `coverage/`
- `__pycache__/`
- 로그 파일
- 임시 파일
- 모델 가중치
- 로컬 캐시
- 실제 환경변수 파일

대용량 파일이나 모델 파일을 생성하기 전에 저장 위치와 `.gitignore` 적용 여부를 확인합니다.

## 8. 안전한 Git 및 파일 작업

작업 시작 전에 확인한 사용자 변경과 이번 작업으로 만든 변경을 구분합니다.

사용자가 명시적으로 요청하지 않는 한 다음 작업은 수행하지 않습니다.

- `git add`
- `git commit`
- `git push`
- tag 생성
- Pull Request 생성
- Pull Request 병합

다음과 같은 파괴적 명령은 사용자 승인 없이 실행하지 않습니다.

```text
git reset --hard
git clean -fd
git restore .
git checkout -- <file>
rm -rf
docker compose down -v
docker system prune
```

파일 삭제나 대규모 이동이 필요한 경우 대상 파일과 영향을 먼저 보고합니다.

## 9. 환경변수와 보안

- API 키를 코드에 하드코딩하지 않습니다.
- DB 접속 정보를 코드나 문서에 실제 값으로 기록하지 않습니다.
- 모델 비밀값과 인증 정보를 Git에 커밋하지 않습니다.
- 실제 값은 로컬 `.env`에서 관리합니다.
- 저장소에는 `.env.example`만 유지합니다.
- 새로운 환경변수가 필요하면 `.env.example`에 키와 설명만 추가합니다.
- 브라우저에 공개되는 환경변수와 서버 전용 환경변수를 구분합니다.
- 로그와 오류 메시지에 비밀값이 노출되지 않도록 합니다.

## 10. 문서와 구현의 일치

코드 또는 설정 변경으로 기존 문서가 틀리게 되는 경우 승인된 작업 범위 안에서 관련 문서를 함께 갱신합니다.

문서에는 다음을 구분해서 작성합니다.

- 현재 실제로 구현된 내용
- 향후 적용할 계획
- 기존 MVP 또는 레거시 구조

확인하지 않은 동작이나 실행하지 않은 검증을 완료된 것처럼 작성하지 않습니다.

## 11. 최종 보고

작업 완료 보고에는 다음 내용을 포함합니다.

1. 변경 목적
2. 적용 결과
3. 수정하거나 이동한 주요 파일
4. 실행한 검증과 결과
5. 실행하지 못한 검증과 이유
6. 기존 오류와 이번 변경에서 발생한 오류의 구분
7. 남은 위험 또는 제한사항
8. 사용자 확인이 필요한 사항

실제 Git 변경이 발생한 경우 다음을 확인합니다.

```bash
git status --short
git diff
```

이번 작업에서 발생한 변경만 기준으로 한글 Conventional Commit 메시지를 제안합니다.

커밋 메시지는 제목만 제안하지 않고, 제목과 본문을 하나의 `text` 코드 블록으로 제공합니다.

형식:

```text
<type>(<scope>): <제목>

- 변경 목적
- 주요 변경 내용
- 검증 결과
- 남은 제한사항
```

서로 다른 목적의 변경이 섞여 있으면 커밋을 분리하도록 안내합니다.
